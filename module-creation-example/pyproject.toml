[tool.poetry]
name = "my_module"
version = "0.1.0"
description = ""
authors = ["j"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
